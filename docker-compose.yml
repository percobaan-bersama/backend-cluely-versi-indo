services:
  postgres:
    image: postgres:16
    container_name: cluely-postgres
    restart: unless-stopped
    env_file:
      - .env
    environment:
      POSTGRES_DB: ${DB_NAME}
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    ports:
      - "${DB_PORT}:5432"
    volumes:
      - cluely-postgres-data:/var/lib/postgresql/data

volumes:
  cluely-postgres-data:
